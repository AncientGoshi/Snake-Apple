# Snake game
## Please execute `execute_functions.py`

_note: the tail is considered to be part of the snake **:)**_

Before running, cd into this directory, and execute 
```shell
source ./.venv/bin/activate
```
and then, to run execute
```shell
python3 execute_functions.py
```